pull_request_rules:
- actions:
    merge:
      commit_message: template
      template_commit_message: '{{ title }} (#{{ number }})

        {{body}}

        super RP!

        '
  conditions:
  - base=20211001092421/test_merge_template/main
  name: merge on main
