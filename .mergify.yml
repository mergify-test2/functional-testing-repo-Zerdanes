pull_request_rules:
- actions:
    merge:
      commit_message: template
      template_commit_message: '{{ title }} (#{{ number }})

        {{body}}

        super RP!

        '
  conditions:
  - base=20210929121401/test_merge_template/main
  name: merge on main
