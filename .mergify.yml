pull_request_rules:
- actions:
    merge:
      commit_message: template
      commit_message_template: '{{ title }} (#{{ number }})

        {{body}}

        superRP!

        '
  conditions:
  - base=20211015101944/test_merge_template/main
  name: merge on main
